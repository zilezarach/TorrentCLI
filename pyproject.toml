[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "yarr-cli"
version = "2.0.0"
description = "🏴‍☠️ Yarr – a beautiful terminal client for torrents, books, movies and games"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "certifi>=2024.0.0",
    "click>=8.1",
    "markdown-it-py>=3.0",
    "Pygments>=2.15",
    "qbittorrent-api>=2024.1",
    "requests>=2.31",
    "rich>=13.0",
    "shellingham>=1.5",
    "typer>=0.9",
    "typing_extensions>=4.5",
    "urllib3>=2.0",
]

[project.scripts]
yarr = "yarr:main"

[tool.setuptools]
py-modules = ["yarr", "zil_api_client"]
